[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers --cov=pyecobee --cov-report=term-missing --cov-fail-under=81
markers =
    live: tests that contact the ecobee API and require credentials
